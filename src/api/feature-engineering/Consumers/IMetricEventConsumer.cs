namespace AFIE.FeatureEngineering.Consumers;
public interface IMetricEventConsumer
{
       // Marker interface. Implementations are also IHostedService, so DI wiring
    // is done via AddHostedService<T> in Program.cs.
}